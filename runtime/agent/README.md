# Agent Runtime

This directory will contain the thin agent-facing execution wrapper.

The wrapper should translate an agent request into one master CLI invocation instead of duplicating orchestration logic.
